import LogoutButton from "@/features/auth/LogoutButton";

const Settings = () => {
  return (
    <section className="flex h-full w-full items-center justify-center">
      <LogoutButton className="bg-custom-bg-1! text-white!" />
    </section>
  );
};

export default Settings;
